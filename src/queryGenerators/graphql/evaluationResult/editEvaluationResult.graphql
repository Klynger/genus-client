#import "./evaluationFragment.graphql"

mutation editEvaluationResult($resultId: ID!, $newResult: Float!) {
  editEvaluationResult(resultId: $resultId, newResult: $newResult) {
    ...evaluationResultFields
  }
}
