#import "./evaluationResultFragment.graphql"

mutation createEvaluationResults($input: [CreateEvaluationResultInput!]!) {
  createEvaluationResults(input: $input) {
    ...evaluationResultFields
    evaluation {
      id
    }
    user {
      id
    }
  }
}
