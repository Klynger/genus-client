mutation createUser($userInput: CreateUserInput!) {
  createUser(input: $userInput) {
    id
    teacherSubjects {
      id
    }
    studentSubjects {
      id
    }
    evaluationResults {
      id
    }
  }
}
